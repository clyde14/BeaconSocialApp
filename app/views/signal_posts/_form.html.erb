<%= form_for(signal_post) do |f| %>
  <% if signal_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(signal_post.errors.count, "error") %> prohibited this signal_post from being saved:</h2>

      <ul>
      <% signal_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
